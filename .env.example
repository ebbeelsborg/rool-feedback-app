# Shared space for multi-user access.
# 1. Create a space in Rool Console (console.rool.dev)
# 2. Set link access to 'editor' so people with the link can access
# 3. Copy the space ID from the URL and set it here, then rebuild
# Users can read all issues, create new ones, and edit only their own.
VITE_ROOL_FEEDBACK_SPACE_ID=
